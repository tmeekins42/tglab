BUGS / SMALL FEATURES
- would a tty / stdio backend make the app easier to debug? Support Windows UIA?
- add faint gridlines around the pixels in the loupe
- at some point we might need a vram garbage collector if we're doing a LOT of gpu operations and collecting a lot of temporary textures.
- auto-develop: AUTO WHITE BALANCE still to do. Auto-exposure is in
  (basic_adjust's auto_exposure parameter, scripts/autodevelop.tgl); the same
  mechanism should carry white balance, but it needs an estimator rather than
  just a measurement. Grey-world is the usual starting point and is often
  wrong on a scene with a dominant colour; white-patch is better when
  something in the frame really is neutral. The camera's own as-shot value is
  already the kelvin/tint default, and it is a decent answer, so an estimator
  has to beat that to be worth switching on.
  Note the "copy of the sliders" worry turned out not to exist: the suggestion
  sets DEFAULTS, so an untouched slider follows it and a dragged one survives
  every re-run. Verified in tests/test_autodev.cpp.
- can we add a gpu watchdog that catches and breaks out of gpu hangs?
- denoise: dust and scratch removal for scanned slides. Different problem from
  ISO noise -- sparse impulse defects rather than broad-band -- so it wants a
  DETECT-then-repair filter shaped like hot_pixel_repair (2D, colour) rather
  than a blur. median_blur handles it today but destroys detail everywhere.
- denoise: level-dependent thresholds, MEASURED and worth doing but not urgent.
  The earlier note here said a variance-stabilising (Anscombe/sqrt) transform
  was "the textbook next step". That was reasoned from the raw mosaic, where
  noise is shot-limited. Measured on the DEVELOPED image, which is what
  wavelet_denoise actually sees, the picture is different:
    sigma across the brightness range, after develop
      _dsc0037 (ISO 6400, pushed +1.82)  0.0061 .. 0.0398  = 6.5x
      _MG_9673 (ISO 1000)                0.0034 .. 0.0060  = 1.8x
      _DSC0162 (ISO 125)                 0.0047 .. 0.0049  = 1.1x
  So one fixed threshold is badly wrong on a heavily pushed high-ISO frame and
  nearly right on everything else -- the sRGB curve has already done most of
  the stabilising.
  And sqrt is the WRONG exponent for that reason. Flatness (max/min of sigma)
  on _dsc0037: raw 6.5x, after sqrt 2.0x, after log 1.8x, after L^0.75 1.2x.
  If this is done, L^0.75 is the transform, not Anscombe.
  Cheaper alternative worth trying first: scale the threshold by the local
  level inside the existing kernel. No transform, no round-trip, and it can be
  a parameter so the effect is visible rather than baked in.
- when adding images to the palette/group, maybe we should pre-load the EXIF
  as well as the thumbnail. EXIF loading should be simple compared to the JPEG.
- add right-click sort by date in the groups, like sort by name. Use EXIF shot
  date, and fall back to file timestamp if there's no EXIF.
- add drag/move to the image palette and group for re-ordering
- demosaic chroma pass is 22ms of the entire ~45ms... worth optimizing this. I think
  several demosaic algorithms use the chroma pass.
- when an image is loading/processing, put a spinner over the thumbnail in the image palette
  if the image is cached, etc, maybe another icon over the thumbnail to show this
  this has no practical purpose, but I think it's nice for the user to watch the processing
  this would be a good time to add an error icon on the thumbnail if the image failed to load
- while processing, update the status on CPU/GPU usage and timing after each stage instead of 
  at the end.

NEW FEATURES / IDEAS
- LOCAL TONEMAP (tonemap_local). The global operator is doing what a global
  operator can: on hdrtest2 the sky keeps its ordering and 1.36 of its 1.89
  stops through a full highlight recovery. It still READS flat, and that is not
  a bug -- the sky spans 1.9 stops while the scene spans 5.8, so it occupies a
  small slice of the range and sits next to much darker land.
  A local operator is the fix: split base from detail with an edge-preserving
  filter (guided_filter is already here), compress the BASE hard, keep the
  detail. Registered separately so the two can be compared in one script, which
  is the point. Watch for halos -- that is the failure mode, and why global is
  worth keeping as the default.
- add support for additional image channels, such as alpha channels and depth channels
- channel merging. Astronomy images are captured single channel with colored filters. How to merge them?
- image export
- batch processing
- an option to visualize clipping regions in the image. can we visual crush as well?
- the image palette is a collection of images. I would like the option to make a palette entry into a image group.
  + in a group, dropping an image adds it to the list instead of replacing
  + we'll need a remove image option (right-click)
  + an image group/list is a new data type. Some algorithms take an image list as input (focus stacking, panorama merging, SfM...)
  + a group has a hidden frame number and a play button can make us cycle the frame number. this does nothing for algorithms that take an image list
  + algorithms that take a single image will use the image at the image index in the group. when the index changes if play is pressed, updates the dirty flag to re-process
  + a video file can be dropped into a group as well
  + for videos, we might need a way to set start/end frames as we probably don't need the entire video.
- Focus stacking
- feature tracking support, numerous algorithms
- features should be associated with the image. Feature tracking is used for matching images, so every image in a group/image list will have a feature list.
- photo alignment from features
- panorama photo merging, multiple projections
- Job system: future algorithms could become much slower and broken into multiple stages, some with dependencies on other stages, some that can run in parallel. A job system can make this faster
- progress bar for job system
- are there any public lens correction databases we could take advantage of?
- can we run a script (develop?) on every image in a group, then run an algorithm, like panorama that works on the entire group? 
  Maybe we can have image/image group in the palette that is the output from another script?
  Or is it better to expand our scripting? so we need iteration?
  Example: merge 3 photos into 1 HDR photo. I have a dozen of the group of 3 HDRs that are then stitched into an HDR panorama.
- can algorithms be toggled on/off?
- algorithm to fix chromatic aberation. can it be detected and auto fixed without analysing the lens?
- have an option to drag a splitter so we can see before/after on a single image.
- create thousands of synthetic images, sample them like a raw sensor, then use deep learning to find a demosaic neural network
- alternate form of nested script functions func1() => func2() => func3(). Many modern 
  computer languages are adopting this as long as the main object we pass is the first argument.
  I think this can be easier to read and understand by non-programmers than nested calls.
- at some point i think we might need an interactive script editor in the app instead of depending
  on an external text editor. It could even be smart with pulldowns for commands and algorithms
  should also support syntax highlighting.

NOTES
- if we need matrix functions, I suggest the Euler library and Ceres for bundle solving

FROM CLAUDE (raised in conversation, not yet in the list above)
- TEST BRACKET FOR THE FINE ALIGNMENT SOLVER: D:\lrwork\TEST\hdrtest2 shows
  visible fringing from roughly 1-pixel frame-to-frame movement. Tim spotted it
  in the merge. That is the sub-pixel solver's case exactly -- too small for
  feature matching to be worth it, large enough to see. Keep this bracket as the
  reference for that work.
- basic_adjust ranges scaling with bit depth. Different raws have different bit
  depths and the controls do not currently know.
- exposure does not visibly stretch the histogram. Suspected to be the SDR
  display window rather than the data: values pushed above 1.0 leave the visible
  range instead of stretching it. Related: showing where the SDR window sits
  within the full range, which Tim suggested as a way to make headroom legible.
  if the histogram shows the entire image, and what is displayed, can we show a window around the displayed area?
- proxy resolution for interactive edits: work at view resolution while a slider
  moves, full resolution when idle. Benched deliberately -- local-neighbourhood
  algorithms give different results at lower resolution, so a proxy is only
  honest for the preview.
- threshold_otsu / _triangle / _isodata have no GPU path, and deriving a level
  from a histogram is why. With BuildHistogram they become build-on-GPU, read
  4 KB, compute the level, dispatch the existing threshold kernel. Two things to
  settle first: whether Otsu on the subsampled histogram picks the same level as
  Otsu on every pixel, and that such a stage cannot batch with its neighbours.
  (This is the same ground as "add GPU implementation for local_threshold".)

ANSWERED, NO ACTION
- mosaic() erroring on a non-raw image: recommended keeping the error, since a
  script asking for sensor data on a JPEG has made a mistake worth reporting.

DONE (recent, pruned periodically)
- brightness: handled only RGBA8 and R32F and silently produced ZEROS on
  anything else -- a demosaiced raw came out black with no error. Now goes
  through PixelBuffer like every other algorithm, so all four formats work.
- brightness sliders renamed: amount -> brightness, gain unchanged.
- PixelBuffer's `default: // RGBA8` in Unpack and PackInto now name every format
  explicitly and complain about an unknown one. That switch sits behind thirteen
  algorithms, so it was the single place a new format would be misread by all of
  them at once.
  NOTE: gaussian_blur, median_blur and sobel were checked and are fine -- they
  branch on format to pick a STRATEGY (single- vs multi-channel), not to decide
  whether to do anything, and the paths that matter go through PixelBuffer.
  Measured on an RGBA16F input: only brightness was broken.
- when a script's viewers are first docked, the LAST display() is now selected
  rather than whatever tab the saved layout had on top. ImGui persists the
  selected tab per dock node in tglab_layout.ini, so hello.tgl kept showing
  "original" and the brightness slider appeared to do nothing -- the visible tab
  was the source image. A tab the user has since chosen is left alone: selection
  only happens on the frame a viewer is first docked.
- grayscale bailed out on anything but RGBA8 (`if (format != RGBA8) return;`),
  leaving its output as allocated -- i.e. BLACK. It is the first stage in
  hello.tgl, so everything downstream went black too and it read as a broken
  brightness slider. Found by Tim, who correctly suspected grayscale rather than
  brightness.
  A registry-wide sweep then found three more, all the same shape: sobel,
  hysteresis and gaussian_blur read half- and 32-bit-float images as BYTES --
  not zeros but plausible-looking nonsense, which is harder to notice. All four
  now handle every format.
  My earlier grep-based survey missed every one of these; only enumerating the
  registry and running each algorithm on each format found them. That sweep is
  now a test.
  STILL OPEN, deliberately: threshold and threshold_bernsen output black on a
  float image because their `level` is fixed at 0..255 while the values are
  0..1. That is the same "ranges do not follow the format" issue as
  basic_adjust's, not a "did not run" bug.